general:
    port: 1488
    daemonize: true

parsers:
   'echo 123':
        type: command
        metrics:
            random:
                pattern: '([0-9]+)'
                type: lastvalue
                interval: 1
